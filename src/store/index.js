import Vue from 'vue'
import Vuex from 'vuex'
import statCanData from '@/assets/tax_amounts.json'

Vue.use(Vuex)

console.log(statCanData);

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
